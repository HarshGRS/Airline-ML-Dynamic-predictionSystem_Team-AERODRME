# Copy to .env for local dev, or set this in Vercel's Project Settings > Environment Variables.

# Local dev points at your local backend. In production, set this to your
# deployed Render URL, e.g. https://airline-pricing-api.onrender.com
REACT_APP_API_URL=http://localhost:8000
